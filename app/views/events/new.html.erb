<!--Restful-->

<!--<%= form_for @event, url: events_path do |f|  %>-->
<!--本例中可以完全省略:url參數 Rails可以根據@event推算出路由-->

<%= form_for setup_event(@event),url: events_path do |f| %>

	<%= render partial: "form", locals: {:f => f} %>
	<%= f.submit "Create" %>
<% end %>